[package]
name = "thouless-python"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT"
publish = false

[lib]
name = "_core"
crate-type = ["cdylib"]
test = false
doctest = false

[dependencies]
pyo3 = { version = "0.23.5", features = ["num-complex"] }
thouless = { path = "../.." }
